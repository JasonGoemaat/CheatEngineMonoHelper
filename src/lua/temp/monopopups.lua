mono.popups.class = mono.popups.class or {}
mono.popups.field = mono.popups.field or {}
mono.popups.method = mono.popups.method or {}

--[[
    Popup on a class will let you open the class and add/edit/remove
    a note.  It is recreated each time when opened.
--]]
function mono.popups.class:update(menu, class, openedClass)
end
